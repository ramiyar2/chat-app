import 'package:flutter/material.dart';
import 'package:flutter/rendering.dart';

class CompleteSigin extends StatelessWidget {
  const CompleteSigin({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
